// import React from "react";
import FolderManager from "../components/FolderManager"; // if you built one earlier

const FolderPage = () => (
  <div>
    <h1>Folder Manager</h1>
    <FolderManager />
  </div>
);

export default FolderPage;
